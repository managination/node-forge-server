#Forge cryptographic library for meteor

This package adds a global `frg` and `forge` variable (`frg === forge`) to give access to the encryption, decryption
and hashing functions from forge on the server.